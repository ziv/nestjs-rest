# nestjs-rest-mongodb

A MongoDB adapter nestjs-rest.

A CRUD service for JSON:API compliant applications using NestJS and MongoDB.

Support multiple pipelines:

- Offset pagination
- Cursor pagination

